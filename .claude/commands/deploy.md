# Deploy

Run the deploy script: `./scripts/deploy.sh $ARGUMENTS`

**Targets:**
- `all` (default) - Deploy everything
- `frontend` / `fe` - Build and deploy hosting
- `backend` / `be` - Deploy Data Connect + Functions
- `functions` / `fn` - Deploy Cloud Functions only
- `dataconnect` / `dc` - Deploy schema only
